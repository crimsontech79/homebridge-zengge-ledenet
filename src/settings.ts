/** Must match the `platform` name used in the Homebridge config.json. */
export const PLATFORM_NAME = 'ZenggeLedenet';

/** Must exactly match the "name" field of package.json. */
export const PLUGIN_NAME = 'homebridge-zengge-ledenet';
